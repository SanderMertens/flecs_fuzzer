using flecs.meta

struct Scalar {
  value = f64
}

struct Metrics {
  source = f64
  doubled = f64
  shifted = f64
}

Config {
  Scalar: {value: 21.5}
}

derived {
  Metrics: {
    source: Config[Scalar].value
    doubled: Config[Scalar].value * 2
    shifted: Config[Scalar].value + 10
  }
}
